Example of a server that can connect to multiple clients simultaneously.
Clients can run on the same machine as the server, a different machine
on the same LAN (or WAN - Wireless network) or over the internet.
